{
  "name": "s02e02",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun --env-file=../.env run index.ts",
    "agent": "bun --env-file=../.env run agent.ts",
    "mcp": "bun --env-file=../.env run mcp-stdio.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "verify-target": "bun --env-file=../.env run scripts/verify-target-masks.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "sharp": "^0.34.5",
    "zod": "^4.3.6"
  }
}

{
  "name": "@ai-devs/s02e04",
  "version": "0.1.0",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "description": "AI Devs 4 — S02E04 mailbox agent (zmail + hub verify)",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "start": "bun --env-file=../.env run run.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  }
}
